name: PASS Slack Report 18 JST

on:
  schedule:
    # GitHub Actions cron is UTC. 09:00 UTC = 18:00 JST.
    - cron: "0 9 * * *"
  workflow_dispatch:
    inputs:
      date_ymd:
        description: "Target date in report timezone, e.g. 2026-06-07. Empty means today in Asia/Tokyo."
        required: false
        default: ""
      force_send:
        description: "true = send even if already sent marker exists."
        required: false
        default: "false"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

concurrency:
  group: pass-slack-report-18jst
  cancel-in-progress: false

jobs:
  send-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci

      - name: Update TLE once per JST day
        run: |
          set -euo pipefail
          TODAY="$(TZ=Asia/Tokyo date +%F)"
          MARKER="reports/tle-updated/${TODAY}.json"

          if [ -f "$MARKER" ]; then
            echo "TLE already checked today: $MARKER"
            exit 0
          fi

          node scripts/update-tle.mjs
          mkdir -p reports/tle-updated
          printf '{\n  "date": "%s",\n  "updated_at_utc": "%s"\n}\n' "$TODAY" "$(date -u +%FT%TZ)" > "$MARKER"

      - name: Send today's PASS report to Slack at 18 JST
        run: |
          set -euo pipefail
          TARGET_DATE="${{ github.event.inputs.date_ymd }}"
          if [ -z "$TARGET_DATE" ]; then
            TARGET_DATE="$(TZ=Asia/Tokyo date +%F)"
          fi

          if [ "${{ github.event.inputs.force_send || 'false' }}" = "true" ]; then
            PASS_FORCE_SEND=true
          else
            PASS_FORCE_SEND=false
          fi

          PASS_REPORT_DATE_YMD="$TARGET_DATE" \
          PASS_FORCE_SEND="$PASS_FORCE_SEND" \
          node scripts/send-pass-report.mjs
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}

      - name: Commit TLE/report markers if changed
        run: |
          if git diff --quiet -- public/tle reports; then
            echo "No TLE/report marker changes."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/tle reports
          git commit -m "Update TLE or PASS report marker"
          git push
